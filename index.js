const userNumbers = prompt ('Please input numbers.');
const userNumbersAsString = userNumbers.split(`,`);
const userNumbersAsNumbers = [];

for (const indexCounter = 0; indexCounter < userNumbersAsString.length; indexCounter = indexCounter + 1);